rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Règles de développement : lecteurs et écritures ouverts pour l'instant.
    // À restreindre avec firebase_auth avant toute mise en production.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}